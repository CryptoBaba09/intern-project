import InterndexView from "./InterndexView";

export const metadata = {
  title: "$interndex — Any-Chain Token Swap That Burns $INTERN",
  description:
    "Swap any token, any chain, into $INTERN or anything else — live now. Every swap's 0.2% fee auto-buys-back and burns $INTERN, confirmed on-chain.",
};

export default function InterndexPage() {
  return <InterndexView />;
}
