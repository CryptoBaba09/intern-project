import VideoCreditsView from "./VideoCreditsView";

export const metadata = {
  title: "AI Video Generation — Burn $INTERN for Real Credits",
  description:
    "Burn $INTERN for real video credit, then spend it on-site to animate Blaze, Rendo, or Promptly. No key to manage — the site generates it for you.",
};

export default function VideoCreditsPage() {
  return <VideoCreditsView />;
}
