import VideoCreditsView from "./VideoCreditsView";

export const metadata = {
  title: "Video Credits",
  description:
    "Burn $INTERN for real video credit, then spend it on-site to animate Blaze, Rendo, or Promptly via Runway or HeyGen. No key to manage — the site generates it for you.",
};

export default function VideoCreditsPage() {
  return <VideoCreditsView />;
}
