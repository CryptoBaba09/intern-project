import MarketplaceView from "./MarketplaceView";

export const metadata = {
  title: "AI Agent Marketplace — Hire an Intern, Burn $INTERN",
  description:
    "Hire an intern. Blaze (autonomous burn engine), Rendo (text + video generation), Promptly (inference credits), Synapse (on-chain connectome), and Hush (cross-chain swap) are live. Perky and Forge are in design; Div is blocked on a real-world precondition — each spending or unlocked by $INTERN.",
};

export default function MarketplacePage() {
  return <MarketplaceView />;
}
