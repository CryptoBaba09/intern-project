import MarketplaceView from "./MarketplaceView";

export const metadata = {
  title: "Marketplace",
  description:
    "Hire an intern. Blaze (autonomous burn engine), Rendo (text + video generation), Promptly (inference credits), and Synapse (on-chain connectome) are live. Perky (loyalty rewards) and Forge (custom builds) are in design; Div (dividend routing) is blocked on a real-world precondition — each spending or unlocked by $INTERN.",
};

export default function MarketplacePage() {
  return <MarketplaceView />;
}
