import MarketplaceView from "./MarketplaceView";

export const metadata = {
  title: "Marketplace",
  description:
    "Hire an intern. Blaze (burn tracker) is live; Rendo (media), Promptly (inference credits), Perky (loyalty rewards), and Div (dividend routing via DRIP) are in design — each spending or unlocked by $INTERN.",
};

export default function MarketplacePage() {
  return <MarketplaceView />;
}
