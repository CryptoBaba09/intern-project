import RosterView from "./RosterView";

export const metadata = {
  title: "Roster Call",
  description:
    "Pitch the next $INTERN persona. Real submissions, stored and read by the team — treasury pays chosen ideas in $INTERN by hand, no automatic payout yet.",
};

export default function RosterPage() {
  return <RosterView />;
}
