import BuybackBurnView from "./BuybackBurnView";

export const metadata = {
  title: "Does Buyback-and-Burn Crypto Actually Work? $INTERN's On-Chain Proof",
  description:
    "Most \"burn\" tokens announce a mechanism once and call it deflationary forever. Here's what $INTERN actually does, with the transaction hashes to check it yourself.",
};

export default function BuybackBurnPage() {
  return <BuybackBurnView />;
}
