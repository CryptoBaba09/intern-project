import RoadmapView from "./RoadmapView";

export const metadata = {
  title: "Roadmap",
  description:
    "Everything $INTERN is built to do — live today (marketplace burns), in progress (staking distributions, digital avatars), and what's next.",
};

export default function RoadmapPage() {
  return <RoadmapView />;
}
